name: hello-world
on: push
jobs:
  my-job:
    runs-on: minhduc_laptop_label_1
    steps:
      - name: my-step
        run: echo "Hello World!"
        env:
          HTTP_PROXY: http://proxy-dmz.intel.com
